# 한국의료분쟁조정중재원 웹진 — 마크업 · 레이아웃 기획서 (웹 에이전트 프롬프트용)

> 이 문서는 웹 에이전트에게 그대로 지시할 수 있는 **프롬프트 단위**로 작성되었습니다.
> 각 섹션의 "지시:" 블록은 에이전트에게 그대로 전달 가능한 실행 지시문입니다.
> 색상·타이포·간격 등 디자인 토큰은 기존 `웹진 제작 지시서.md` 5장(디자인 가이드) 그대로 따릅니다 — 이 문서에서는 반복하지 않고 마크업 구조·레이아웃·인터랙션에만 집중합니다.

---

## 0. 이번 기획서의 핵심 결정사항 (요약)

기존 계획(섹션당 3개 + "전체보기" 링크 → 별도 목록 페이지)에서 아래처럼 변경합니다.

| 항목 | 기존안 | **변경된 최종안** |
|---|---|---|
| 메인페이지 범위 | 카테고리별 하이라이트만 | **모든 메뉴 · 모든 누적 게시물이 메인페이지 안에 전부 노출** |
| 페이지 길이 | 짧게 유지 | **길어져도 무방** (세로 스크롤 블로그 형식) |
| 누적 메뉴 노출 개수 | 3개 | **5개** |
| 5개 초과 시 | "전체보기" 링크로 별도 페이지 이동 | **섹션 하단 "더보기" 버튼 클릭 → 같은 섹션 안에서 5개씩 추가로 펼쳐짐 (페이지 이동 없음)** |
| 비누적 메뉴(정적 하위 페이지) | 그대로 노출 | 동일 — 5개 이하라 더보기 불필요 |

---

## 1. 전역 공통 컴포넌트

### 1.1 헤더

**지시:**
> 헤더는 다음 구조로 마크업해줘.
> - 좌측 로고("잇다" + 하단에 작은 글씨로 "한국의료분쟁조정중재원 웹진"), 클릭 시 index.html로 이동
> - 데스크톱 내비게이션: `01 신뢰로 잇다 · 02 제도로 잇다 · 03 마음으로 잇다 · 04 나눔으로 잇다` 4개, 로고 오른쪽에 가로 배치
> - 2뎁스 메뉴는 hover 시 드롭다운 노출. 3뎁스가 있는 경우(신뢰로잇다의 "인사말"/"의료중재원 소개", 제도로잇다의 "의료분쟁 주요 제도 안내") 드롭다운 안에서 소제목(그룹 라벨) + 들여쓰기로 구분
> - 모바일(900px 미만): 햄버거 버튼(3줄 → X 애니메이션) + 하단 아코디언 메뉴로 전환. 카테고리 탭하면 하위 메뉴가 펼쳐짐
> - `position: sticky; top: 0;`, 하단에 2px 실선 구분선
> - **헤더 블록 안에는 내비게이션 아래에 1.3 이슈 헤더(히어로)를 포함한다.** 즉 헤더는 "로고+내비게이션" 한 줄로 끝나는 게 아니라, 그 아래 이슈 헤더까지 이어지는 하나의 공통 블록이며, 이 전체가 **모든 페이지 상단에 동일하게 반복**된다.
>
> **[보류]** 로고를 가운데 두고 좌측에 신뢰/제도, 우측에 마음/나눔을 배치하는 안(잡지형 헤더)은 별도 결정 후 반영 — 지금은 로고-좌측 고정 구조로 작성.
> **[보류]** 헤더를 모든 페이지에서 공통 관리하는 방식(정적 include vs. JS 삽입 vs. PHP include)은 기술 스택 결정 후 확정 — 지금은 페이지마다 동일한 마크업(이슈 헤더 포함)을 복제하는 현재 방식 유지.

### 1.2 푸터

**지시:**
> 푸터는 3단 구성으로 마크업해줘.
> - 1열: 기관명(국문/영문)
> - 2열: 발행 정보 — "창간호" / 발행일
> - 3열: 이전 회차 — 지금은 "창간호로, 이전 회차가 없습니다" 문구, 2호부터는 회차 목록 링크로 대체
> - 하단에 카피라이트 한 줄, 위에 얇은 구분선

### 1.3 이슈 헤더 (히어로) — ✅ 전 페이지 공통 (헤더에 포함)

**[결정됨]** 이슈 헤더는 메인페이지 전용이 아니라 **헤더의 일부로서 모든 하위 페이지에도 동일하게 노출**된다. 즉 `trust/greeting.html`, `share/qna.html` 같은 상세/목록/Q&A 페이지를 열어도 상단에 헤더 내비게이션 바로 아래 이 이슈 헤더가 그대로 반복된다.

Increment.com의 `.IssueHeader` 구조를 참고해 이미 구현된 컴포넌트를 그대로 재사용.

**요약 구조:** 로고에서 내려오는 세로선 → `창간호 / 2025.08.01` (2단 메타) + `신뢰에서 나눔까지` (타이틀)로 이뤄진 테두리 박스 → 중앙 정렬 소개 문단 → "인사말 더 보기 →" 링크 → 발행처 표기.

**지시:**
> 지금 `index.html`에만 있는 `.issue-header` 블록을, 다른 모든 페이지(`trust/greeting.html`, `trust/about.html`, `heart/cases/index.html`, `share/qna.html` 등)의 헤더 내비게이션 바로 아래에도 동일하게 복제해줘. 내용(창간호/날짜/타이틀/인트로 문단/더보기 링크)은 페이지마다 다르지 않고 전부 동일하게 유지한다.
> 다만 `trust/greeting.html` 페이지 자기 자신에서는 "인사말 더 보기" 링크가 자기 자신을 가리키게 되므로, 이 페이지에서만 그 링크를 생략하거나 비활성 텍스트로 바꾸는 것을 고려한다 (작은 디테일이므로 우선순위 낮음).

#### 1.3.1 회차(호)가 여러 개로 늘어남 → Swiper.js 슬라이더로 전환

매달 새 회차가 1개씩 늘어나는 구조라, 지금처럼 창간호 하나만 고정으로 박아두면 안 되고 **회차를 넘겨볼 수 있는 슬라이더**가 필요합니다. 지금은 창간호 1개뿐이지만, 앞으로의 구조를 미리 검증할 수 있도록 **예시로 3개 회차를 추가해 총 4개**를 넣어서 작업합니다.

**지시:**
> `.issue-header` 안의 이슈 칩(`.issue-chip-wrap` + `.issue-chip` + `.issue-intro`)을 **Swiper.js**로 감싸서 회차별로 슬라이드 전환이 가능하게 만들어줘.
> - Swiper.js는 CDN으로 로드한다 (`swiper-bundle.min.css` / `swiper-bundle.min.js`). 빌드 도구 없이 `<script>`/`<link>` 태그만으로 동작하므로 "빌드 없음" 원칙과 충돌하지 않는다. (단, 이 프로젝트에 들어가는 **최초의 외부 JS 라이브러리**이므로 CDN 실패 시 대비한 폴백은 고려하지 않아도 됨 — 단순 CDN 로드로 충분.)
> - 슬라이드 1장 = 회차 1개. 각 슬라이드 안에는 기존 `.issue-chip`(회차/날짜 2단 meta + 타이틀) + `.issue-intro`(소개 문단 + 더보기 링크)가 그대로 들어간다. 구조와 스타일은 기존 `.issue-header` 컴포넌트를 그대로 재사용하고, Swiper는 그 바깥을 감싸는 슬라이드 컨테이너 역할만 한다.
> - 기본으로 보이는 슬라이드는 **가장 최신 회차**(마지막 슬라이드)로 시작한다. 좌우 화살표 또는 스와이프로 이전 회차들을 계속 넘겨볼 수 있어야 하며, 이전 회차가 화면에서 완전히 사라지거나 접근 불가능해지면 안 된다 (요구사항: "최신호만 보이면 안 됨").
> - 하단에 pagination(점 인디케이터, accent 컬러) 또는 "4 / 4" 형태의 카운터를 추가한다. 좌우 화살표 버튼은 라운드 없이 사각형 + 2px 테두리로, 기존 디자인 톤 유지.
> - 로고에서 내려오는 세로 연결선은 슬라이더 전체의 정중앙 상단에 고정으로 유지(슬라이드가 바뀌어도 위치 고정).
>
> **예시 회차 데이터** (아직 확정 콘텐츠 아님 — 자리 채우기용 플레이스홀더, 실제 제목/날짜는 추후 확정):
> | 순서 | 회차 | 날짜 | 타이틀(예시) |
> |---|---|---|---|
> | 1 | 창간호 | 2025.08.01 | 신뢰에서 나눔까지 |
> | 2 | 2호 | 2025.09.01 | 제도를 다시 보다 (예시) |
> | 3 | 3호 | 2025.10.01 | 마음을 듣다 (예시) |
> | 4 | 4호 | 2025.11.01 | 나눔을 잇다 (예시) |
>
> 이 컴포넌트는 헤더에 포함되어 전 페이지 공통이므로, 어느 페이지를 열어도 동일한 4장짜리 회차 슬라이더가 반복 노출된다.

### 1.4 재사용 컴포넌트 — 포스트 리스트 아이템

**지시:**
> 메인페이지의 모든 섹션에서 공통으로 쓰는 글 항목 하나는 다음 구조로 마크업해줘.
> ```html
> <a class="post-list-item" href="{링크}">
>   <span class="post-list-tag">{상위 그룹명, 없으면 생략}</span>
>   <h3 class="post-list-title">{글 제목}</h3>
>   <p class="post-list-excerpt">{한 줄 요약}</p>
> </a>
> ```
> 항목 사이는 1px 구분선, hover 시 제목만 accent 색으로 바뀜. 기존 `post-list`/`post-list-item` 클래스 그대로 재사용.

### 1.5 재사용 컴포넌트 — "더보기" 버튼

**지시:**
> 누적 콘텐츠 섹션 하단에 들어가는 "더보기" 버튼은 다음 규칙으로 마크업 + Vanilla JS로 구현해줘.
> ```html
> <div class="post-list" data-reveal-step="5">
>   <!-- 처음 5개: 그대로 노출 -->
>   <a class="post-list-item">...</a>
>   ... (5개)
>   <!-- 6번째부터: hidden 속성 또는 class="is-more"로 감춰서 DOM엔 이미 존재 -->
>   <a class="post-list-item is-more">...</a>
>   ...
> </div>
> <button class="more-btn" type="button" data-target="{섹션 id}">더보기</button>
> ```
> - 클릭 시 `.is-more` 항목 중 다음 5개의 클래스를 제거해 노출 (fetch나 서버 호출 없이, 이미 DOM에 렌더링된 항목을 순서대로 보여주는 방식)
> - 더 이상 감춰진 항목이 없으면 버튼 자체를 `display:none` 처리
> - 버튼 스타일: 본문 텍스트와 동일 폭 좌측 정렬, 2px 상단 구분선, 클릭 시 accent 색 텍스트로 hover
> - 버튼 라벨은 고정 텍스트 "더보기" (숫자 카운트 표기는 넣지 않음)

---

## 2. 메인페이지(index.html) 레이아웃

### 2.0 전체 구조 개요 — ⚠️ 01~04 메뉴 순서가 아니라 "중요도 순"

메인페이지는 상단 메뉴(01 신뢰로 잇다 → 04 나눔으로 잇다) 순서를 그대로 따르지 않습니다. 대신 아래처럼 **콘텐츠 중요도 순으로 재배열**합니다. 각 섹션이 원래 어느 메뉴 소속인지는 태그(`post-list-tag`)로만 표시되고, 섹션 등장 순서 자체는 이 중요도 순위를 따릅니다.

```
[헤더 — 내비게이션 + 이슈 헤더(회차 Swiper 슬라이더), 전 페이지 공통]

[1순위] 진료과별 의료분쟁 조정 사례       ← 원소속: 03 마음으로 잇다
[2순위] 의료분쟁 주요 통계 현황           ← 원소속: 03 마음으로 잇다
[3순위] 의료중재원 이용 후기              ← 원소속: 04 나눔으로 잇다
[4순위] 의료중재원 소식                   ← 원소속: 04 나눔으로 잇다
[5순위] 전문가 칼럼·인터뷰                ← 원소속: 02 제도로 잇다
[6순위] 바로가기 링크 모음 (그냥 페이지들) ← 원소속: 01 신뢰로 잇다 + 02 제도로 잇다 + 04 나눔으로 잇다 일부
         ├─ 인사말
         ├─ 개요·연혁
         ├─ 주요 사업 안내
         ├─ 본원·지원 소개
         ├─ 2025년 신규·변경 제도
         ├─ 1. 당사자 조력제도
         ├─ 2. 의료분쟁 옴부즈만 제도
         ├─ 3. 불가항력 의료사고 보상제도
         └─ 의료분쟁 Q&A
[7순위] 의료사고 대응 가이드 (파일 다운로드 섹션) ← 원소속: 04 나눔으로 잇다

[푸터]
```

길이 제한 없음 — 세로로 계속 이어지는 블로그형 단일 페이지.

### 2.1 [1순위] 진료과별 의료분쟁 조정 사례

**지시:**
> 메인페이지의 **가장 첫 콘텐츠 섹션**으로 배치해줘 (이슈 헤더 바로 다음). 누적 + 필터 콘텐츠.
> 1. 섹션 상단에 필터 버튼 4개(`전체 / 내과계 / 외과계 / 기타계`) 배치 — 기존 `filter-bar`/`filter-btn` 재사용.
> 2. 필터가 "전체"인 상태에서 최신 5개 노출, 나머지는 `.is-more`로 감춰서 "더보기" 버튼으로 5개씩 추가 노출.
> 3. 특정 진료과 필터를 누르면, 해당 필터에 맞는 항목만 다시 5개 기준으로 재계산해서 노출 (필터링 후에도 5개 초과 시 더보기 버튼 유지, 5개 이하면 더보기 버튼 숨김).
> 4. 기존 `filter.js`에 있는 필터 로직과 더보기 로직이 함께 동작하도록 JS를 확장해줘 (필터 변경 시 더보기로 펼쳐졌던 항목은 다시 접힌 상태로 초기화).
> 5. 리스트 자체는 세로 리스트로 유지한다 (가로 슬라이드/캐러셀 아님 — 슬라이드는 위쪽 이슈 헤더에서 회차를 넘길 때만 쓰고, 글 목록 자체엔 지시서 4장의 "슬라이드·캐러셀 금지" 규정을 그대로 적용).

### 2.2 [2순위] 의료분쟁 주요 통계 현황

**지시:**
> 누적. 현재 1개("2025년 의료분쟁 주요 통계 현황")뿐이므로 더보기 없이 단일 노출, 5개 넘어가면 1순위 섹션과 동일한 더보기 로직 자동 적용. 통계 수치는 인터랙티브 인포그래픽이 아닌 정적 텍스트/단순 CSS 바 형태로 표현(지시서 4장 규정 유지).

### 2.3 [3순위] 의료중재원 이용 후기

**지시:**
> 누적, 현재 2개("우리들의 이야기 1", "우리들의 이야기 2"). 더보기 없이 그대로 노출, 5개 넘어가면 더보기 자동 적용. 태그는 공통으로 "의료중재원 이용 후기".

### 2.4 [4순위] 의료중재원 소식

**지시:**
> 누적, 현재 1개("구성원 이야기"). 더보기 없이 단일 노출, 5개 넘어가면 더보기 자동 적용.

### 2.5 [5순위] 전문가 칼럼·인터뷰

**지시:**
> 누적 콘텐츠 섹션. 현재 게시물 0개이므로, `post-list-item` 대신 빈 상태 문구를 보여줘: "하반기 게재 예정입니다." + "하반기 예정" 태그. 게시물이 실제로 쌓이기 시작하면 5개 노출 + 더보기 로직으로 자동 전환.

### 2.6 [6순위] 바로가기 링크 모음 — "그냥 페이지"들의 깔끔한 링크 디자인

이 섹션에 들어가는 9개 항목은 전부 누적이 아닌 **1회성 정적 페이지**입니다. 다른 섹션처럼 태그+제목+요약 풀세트를 반복하면 무거워 보이므로, 더 가볍고 압축된 전용 디자인을 새로 만듭니다.

**지시:**
> `post-list-item`과는 다른 새 컴포넌트 `.quick-link-grid` / `.quick-link-item`을 만들어줘.
> - 데스크톱: 2~3열 그리드. 모바일: 1열.
> - 각 항목은 **제목 + 화살표 아이콘(→)만** 표시 (요약 문단 없음, 태그도 생략 — "그냥 페이지"라는 성격에 맞게 최대한 담백하게).
> - 카드가 아니라 얇은 1px 구분선으로만 구획, 배경은 기본 bg 그대로(별도 surface 채움 없음).
> - hover 시 텍스트 accent 색 + 화살표 오른쪽으로 살짝 이동.
> - 포함 항목 9개: 인사말 / 개요·연혁 / 주요 사업 안내 / 본원·지원 소개 / 2025년 신규·변경 제도 / 1.당사자 조력제도 / 2.의료분쟁 옴부즈만 제도 / 3.불가항력 의료사고 보상제도 / 의료분쟁 Q&A.
> - 더보기 버튼 없음 (개수가 늘어나지 않는 고정 목록이므로).

### 2.7 [7순위] 의료사고 대응 가이드 — 파일 다운로드 섹션

이 섹션만 다른 섹션들과 성격이 다릅니다 — 글로 읽는 콘텐츠가 아니라 **PDF 파일을 내려받는 용도**이므로, 전용 다운로드 카드로 디자인합니다.

**지시:**
> `.download-card` 컴포넌트를 새로 만들어줘.
> - 좌측: 파일 형식 배지("PDF" 텍스트, accent 테두리 박스)
> - 중앙: 파일명("의료사고 대응 가이드")+한 줄 설명("의료사고 발생 시 상황별 대응 방법을 단계별로 정리한 실용 가이드")+파일 용량(예: "2.4MB", 실제 파일 연동 전까지는 표기 생략 가능)
> - 우측: "다운로드" 버튼, `download` 속성이 붙은 `<a>` 태그로 구현 (2px 테두리, 라운드 없음, hover 시 배경 accent 틴트)
> - 카드 전체는 2px 테두리 박스, surface 배경 채움, 다른 섹션과 달리 리스트가 아니라 **단일 카드 하나**로 끝남.
> - **(확인 필요)** 실제 PDF 파일 에셋이 아직 없으므로, 지금은 `href="#"` placeholder로 마크업만 잡아두고, 실제 파일이 오면 링크만 교체하는 구조로 작성해줘.

### 2.8 반응형 규칙

**지시:**
> - 데스크톱(900px 이상): 1~5순위 섹션 내부는 1단 세로 리스트 유지(카드 그리드 아님 — 블로그 리스트 형식이므로). 6순위 바로가기 링크 모음만 2~3열 그리드.
> - 태블릿/모바일: 리스트 섹션은 동일한 1단 유지, 6순위 그리드는 1열로 전환, 좌우 여백만 축소.
> - 더보기 버튼은 모바일에서도 최소 44px 터치 영역 확보.
> - 필터 버튼(진료과별 사례)은 모바일에서 가로 스크롤 또는 줄바꿈 허용.
> - 이슈 헤더 Swiper는 모바일에서도 스와이프 제스처로 그대로 동작, 화살표 버튼은 터치 영역 44px 이상 유지.
> - 다운로드 카드(7순위)는 모바일에서 좌/중/우 가로 배치를 세로 스택으로 전환.

---

## 3. "더보기" 버튼 — JS 동작 명세 (에이전트 구현용)

**지시:**
> `assets/js/reveal.js` 파일을 새로 만들고, 다음 로직을 Vanilla JS로 구현해줘 (신규 파일, 빌드 도구 없이 순수 JS).
>
> 1. `.post-list[data-reveal-step]`을 가진 모든 섹션을 찾는다.
> 2. 각 섹션 안에서 `.is-more` 클래스가 붙은 항목 개수를 센다. 0개면 "더보기" 버튼을 아예 렌더링하지 않는다(또는 숨긴다).
> 3. "더보기" 버튼 클릭 시, `data-reveal-step`(기본값 5)만큼 `.is-more` 항목에서 클래스를 제거해 순서대로 보이게 한다.
> 4. 더 감춰진 항목이 없으면 버튼을 `display:none` 처리한다.
> 5. 진료과별 사례처럼 필터와 함께 쓰이는 섹션은, 필터 변경 이벤트가 발생하면 더보기로 펼쳤던 항목을 다시 초기 상태(5개만 노출)로 리셋한다.
>
> 서버 요청이나 fetch 없이, 이미 HTML에 전부 렌더링된 항목을 보이기/감추기만 하는 방식으로 구현 — 회차가 누적돼도 정적 HTML 생성 방식은 그대로 유지된다.

---

## 4. 확인 필요 / 보류 사항 모음

- [ ] 헤더 로고 위치(좌측 고정 vs. 가운데 배치) 최종 확정
- [ ] 헤더/푸터 공통화 방식 — 페이지별 복제 유지 vs. PHP include(header.php) vs. JS 삽입 방식 결정 (이슈 헤더 Swiper도 이 공통화 범위에 포함됨)
- [x] ~~이슈 헤더(히어로)를 모든 페이지 공통으로 넣을지~~ → **결정: 모든 페이지 공통, 헤더에 포함**
- [x] ~~"진료과별 조정 사례" 슬라이드 형식 언급~~ → **결정: 글 목록은 세로 리스트+더보기 유지, 슬라이드는 이슈 헤더(회차 전환)에만 Swiper.js로 적용**
- [ ] 각 카테고리별 별도 목록 페이지(trust/, heart/cases/ 등)를 계속 유지할지, 메인페이지가 전체를 커버하니 축소/삭제할지
- [ ] Swiper.js 회차 예시 데이터(2호~4호 제목 "제도를 다시 보다" 등)는 전부 플레이스홀더 — 실제 제목/날짜 확정 필요
- [ ] 의료사고 대응 가이드 PDF 실제 파일 에셋 필요 (현재 `href="#"` placeholder)
